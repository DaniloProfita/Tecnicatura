Sintaxis similares entre Java y JavaScript

Operadores Simplificados/Compuestos
+=
-=
*=
/=
%=
**=

Operadores Aritmeticos
+
-
*
/
% //Residuo
** //Exponente

Operadores de asignacion
=
Los operadores simplificados tambien se asignan

Operadores de comparacion
== Revisa el valor sin importar el tipo de dato
=== Revisa el tipo y el valor(boolean)

Documentar
// = Para una linea de comentario
/* */ = Para multiples lineas de comentario

Secuencias de escape
\' = Mostrar una comilla simple dentro de una cadena en la salida de informacion
\" = Mostrar doble comilla dentro de una cadena en la salida de informacion
\\ = Barra invertida, diagonal inversa, es para mostrar una bacj slash
\n = salto de linea
\r = retorno
\t = tabulacion 
\b = retroceso
\f = salto de pagina

Operador de aumento
++
Ejemplo:    let z = ++a; //PreIncremento
            let z = a++; //PosIncremento

Operador de decremento
-- //Tambien se aplica el Pre y Pos Incremento

Operador Distinto, diferente(boolean)
!=  //Comprueba el valor no el tipo
!== //Comprueba el valor y tipo

Operadores Relacionales(Boolean)
>
<
>=
<=